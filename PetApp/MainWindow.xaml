<Window x:Class="PetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PetApp"
        mc:Ignorable="d"
        Title="Pet Interaction App" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Add New Pet" Click="AddPet_Click" Width="100" Margin="5"/>
            <Button Content="Play with Random Pet" Click="PlayRandom_Click" Width="150" Margin="5"/>
            <Button Content="Reset All" Click="Reset_Click" Width="100" Margin="5"/>
        </StackPanel>

        <DataGrid x:Name="PetsDataGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Select" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Mood" Binding="{Binding Mood, StringFormat={}{0}%}" Width="Auto"/>
                <DataGridTextColumn Header="Energy" Binding="{Binding Energy}" Width="Auto"/>
                <DataGridTextColumn Header="Hunger" Binding="{Binding Hunger}" Width="Auto"/>
                <DataGridTextColumn Header="Weight" Binding="{Binding Weight, StringFormat={}{0} kg}" Width="Auto"/>
                <DataGridTextColumn Header="Fluffiness" Binding="{Binding Fluffiness}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="10" TextWrapping="Wrap"/>
    </Grid>
</Window>